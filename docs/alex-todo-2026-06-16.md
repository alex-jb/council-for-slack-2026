# Alex manual steps — 2026-06-16

> Days 10 + 11 code is shipped + deployed. These two manual steps unlock them in Slack.

## Step 1 — Apply migration 003 (Day 11 workspace stats RPC) — 2 min

The bot has no schema-mutating creds (by design). Apply via Dashboard SQL Editor.

1. Open Supabase Dashboard → council-for-slack project → SQL Editor
2. New query → paste the entire contents of `migrations/003_workspace_stats.sql`
3. Run
4. Verify: `select public.council_workspace_stats('T0BAKDLM11R');` returns 1 row with `calibration_label` column (will be `pending` if no resolved decisions yet)

If it errors with `function already exists`, that's fine — `create or replace` handles re-runs.

## Step 2 — Re-install Slack app with Day 10 manifest (Workflow Builder function) — 5 min

The `functions:` block is new in the manifest. Slack registers Workflow Builder custom functions at install time, so the app needs a fresh install.

### 2a. Update manifest at api.slack.com

1. Open https://api.slack.com/apps/A0BAVEM5SS0
2. Left sidebar → **App Manifest**
3. Switch to **YAML** tab
4. Replace entire content with the manifest below (this includes all 11 days of features — slash commands × 2, shortcut, **functions × 1 (new)**, OAuth scopes, etc.)
5. **Save Changes** at top right — Slack validates. If validator complains about `slack#/types/channel_id`, fall back to `type: string` for `channel_id` and `requester_id` (function still works, judge UX slightly degraded)

```yaml
display_information:
  name: Council
  description: 5 voices, 1 verdict, every voice scored against reality. The second-opinion ritual native in Slack.
  background_color: "#1A1A1A"

features:
  bot_user:
    display_name: Council
    always_online: true
  slash_commands:
    - command: /council
      url: https://council-for-slack.vercel.app/slack/events
      description: Convene a 5-persona council to deliberate a decision
      usage_hint: "[your decision question]"
      should_escape: false
    - command: /council-audit
      url: https://council-for-slack.vercel.app/slack/events
      description: Show this workspace's Brier-calibrated decision history
      should_escape: false

oauth_config:
  redirect_urls:
    - https://council-for-slack.vercel.app/slack/oauth_redirect
  scopes:
    bot:
      - commands
      - chat:write
      - chat:write.public
      - users:read
      - channels:history
      - channels:read
      - canvases:write
      - canvases:read

settings:
  event_subscriptions:
    request_url: https://council-for-slack.vercel.app/slack/events
    bot_events:
      - app_mention
      - message.im
  interactivity:
    is_enabled: true
    request_url: https://council-for-slack.vercel.app/slack/events
  org_deploy_enabled: false
  socket_mode_enabled: false
  token_rotation_enabled: false

shortcuts:
  - name: Send to council
    type: message
    callback_id: send_to_council
    description: Get a 5-persona AI council to deliberate on this message

functions:
  council_deliberate:
    title: Council deliberate
    description: Convene a 5-persona AI council on a decision and post the verdict to a channel.
    input_parameters:
      question:
        type: string
        title: Question
        description: The decision question to deliberate
        is_required: true
      channel_id:
        type: slack#/types/channel_id
        title: Channel
        description: Where to post the council verdict
        is_required: true
      domain:
        type: string
        title: Domain
        description: founder (default) / engineer / investor / career / product / quant
        is_required: false
      context:
        type: string
        title: Context (optional)
        description: Ground truth — numbers, constraints, dates — improves score quality
        is_required: false
      requester_id:
        type: slack#/types/user_id
        title: Requested by
        description: User who triggered the workflow (for attribution on the canvas log)
        is_required: false
    output_parameters:
      verdict:
        type: string
        title: Verdict
        description: GO / WAIT / KILL / SPLIT
        is_required: true
      recommendation:
        type: string
        title: Recommendation (lowercase)
        description: go / wait / kill / split — for downstream conditional steps
        is_required: true
      agreement_score:
        type: number
        title: Agreement score
        description: 0-1, how much the 5 voices align
        is_required: true
      consensus:
        type: string
        title: Consensus
        description: 1-paragraph consensus quote
        is_required: true
      summary:
        type: string
        title: Summary
        description: One-line summary fit for downstream Slack message steps
        is_required: true
      decision_id:
        type: string
        title: Decision ID
        description: Persistent ID — for later /council-audit or resolve workflow
        is_required: false
```

### 2b. Re-install to AJ Bot workspace

1. Left sidebar → **Install App**
2. Click **Reinstall to Workspace** (or "Install" if it shows that)
3. Click **Allow** on consent screen
4. Bot token may change. Copy the new `xoxb-...` token if shown.

### 2c. Update Vercel env if bot token changed

```bash
cd /Users/alexji/Desktop/council-for-slack-2026/web
~/.local/node/bin/vercel env rm SLACK_BOT_TOKEN production --yes 2>/dev/null
printf "xoxb-NEW-TOKEN-HERE" | ~/.local/node/bin/vercel env add SLACK_BOT_TOKEN production --yes
~/.local/node/bin/vercel --prod --force --yes
```

If the token is unchanged (Slack sometimes reuses it on reinstall), skip 2c.

## Step 3 — Smoke test all 3 surfaces — 5 min

### 3a. Slash command (existing)

```
/council should we ship the council Workflow Builder demo on a Tuesday or Friday?
```

Expect: verdict in channel ~10s.

### 3b. Workflow Builder function (NEW today)

1. Click ⚡ (lightning bolt) at top of any channel → **Build a workflow**
2. **From scratch** → trigger: pick anything, e.g. "When a person reacts with an emoji"
3. **Add step** → **Custom** tab
4. Search **"Council deliberate"** — should appear (if not, manifest didn't save the functions: block; re-check Step 2a)
5. Add it. Map inputs:
   - `question` → hard-code a string like `"should we ship today?"` OR use `{{trigger.message_text}}` if reaction trigger
   - `channel_id` → `{{trigger.channel}}` or hard-code a channel ID
6. **Publish**, then fire the trigger (react with whatever emoji you picked).
7. Expect: verdict posts to the channel, ~10s.

### 3c. /council-audit with Day 11 calibration header

```
/council-audit
```

Expect: top of the audit shows either:
- `:dart: Workspace calibration: excellent — avg Brier 0.083 across 3 resolved...` (if you have resolved decisions), or
- `:hourglass_flowing_sand: Workspace calibration: pending — resolve a decision below to unlock your team's Brier score`

If you get an error `function council_workspace_stats does not exist`, Step 1 migration didn't apply.

## Step 4 — Take screenshots for Devpost — 10 min

For the submission, capture:

- `/council` verdict in channel showing 5 voices
- `/council-audit` showing the new calibration header
- Workflow Builder custom function picker with "Council deliberate" visible
- Channel Canvas tab showing the decision log entries

Save to `docs/screenshots/` and commit.

## When done

Reply to Claude: "manifest reinstalled" or "smoke test passed". Then we move to Loom recording (script ready at `docs/loom-script.md`).
